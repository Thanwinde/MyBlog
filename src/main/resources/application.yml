spring:
  thymeleaf:
    cache: true                     # 开启模板缓存（默认值： true ）
    check-template: true            # 检查模板是否存在，然后再呈现
    check-template-location: true   # 检查模板位置是否正确（默认值： true ）
    content-type: text/html         # Content-Type 的值（默认值： text/html ）
    enabled: true                   # 开启 MVC Thymeleaf 视图解析（默认值： true ）
    encoding: UTF-8                 # 模板编码
    excluded-view-names: ""         # 要被排除在解析之外的视图名称列表，用逗号分隔
    mode: HTML5                     # 要运用于模板之上的模板模式。（默认值： HTML5）
    prefix: classpath:/templates/   # 在构建 URL 时添加到视图名称前的前缀（默认值： classpath:/templates/）
    suffix: .html                   # 在构建 URL 时添加到视图名称后的后缀（默认值： .html ）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:11451/myblog?useSSL=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: TWind
    password: UserPassisnsh250.
#    username: root
#    password: 123456
  mail:
    host: smtp.qq.com
    port: 587
    username: 3204789832@qq.com
    password: nmeplvjhyafldebc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
htmls:
  blogs: "E:/IT/MyBlogs/blogs/"
  index: "E:/IT/MyBlogs/"
#  blogs: "/root/MyBlogs/blogs/"
#  index: "/root/MyBlogs/"


